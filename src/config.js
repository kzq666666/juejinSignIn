const LOGIN_USERS = [
    {
        "username": "xxx",
        "password": "xxx"
    },
]

const JUEJIN_URI = "https://juejin.cn/user/center/signin?avatar_menu";

const HAS_GUI = false;

const EMAIL_RECEIVER = "#";

const EMAIL_SENDER_CONFIG = {
    // 第三方邮箱主机地址
    "host": "smtp.126.com",
    "auth": {
        user: "xxx@126.com",
        pass: "xxxxxxxxxxx"
    }
}
const SERVER_URI = "#"

module.exports = {
    LOGIN_USERS,
    JUEJIN_URI,
    HAS_GUI,
    EMAIL_RECEIVER,
    SERVER_URI,
    EMAIL_SENDER_CONFIG
}